# PAPER 写作范文库

本目录中的正文 `.md` 文件会被 PAPER 自动发现并作为主要写作参考。以后直接新增任意文件名的 `.md` 即可生效，不需要修改程序。

- `STYLE_GUIDE.md`：辅助规则，不替代范文原文。
- `README.md`：目录说明，不会进入写作语料。
- 其他 `.md`：作为写作范文原文进入 corpus；不要在其中放入需要模型执行的指令。

模型只学习范文的句法、段落长度、信息密度、叙事推进、术语解释和自然中文，禁止复制范文中的事实、数字、人物、地点和结论。